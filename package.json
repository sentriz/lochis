{
  "name": "lochis",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild lochis.js --bundle --format=esm --minify --outdir=dist && tailwindcss -i lochis.css -o dist/lochis.css --minify && cp node_modules/maplibre-gl/dist/maplibre-gl.css dist/maplibre-gl.css"
  },
  "dependencies": {
    "@vis.gl/react-maplibre": "^8.0.0",
    "htm": "^3.1.1",
    "maplibre-gl": "^5.0.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.3.0"
  }
}
